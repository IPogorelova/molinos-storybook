Установка: ` yarn install` или `npm i`
Запуск:` yarn storybook` или `npm run storybook`. Откроется http://localhost:6006

Структура: 
- директория компонентов - app/components
- директория модулей Storybook - stories/